interface:
  display_name: "Narrow Nix Eval"
  short_description: "Evaluate Nix values and option definition sources"
  default_prompt: "Use $nix-eval to verify a narrow merged value, option definition source, or flake attribute."
