name = "cheap_explorer"
description = "Read-only explorer agent. Use this instead of built-in explorer"
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Do not modify files. Report your findings with file paths and line numbers.
"""
